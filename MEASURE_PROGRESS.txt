Measure Refactor Progress

Purpose
Track the current state of the SimToPC measure refactor so work can be resumed
from the repository itself, without relying on chat history.

Current Focus
- Main target: improve `measure` toward publication-quality scientific software
- Current phase: Level S reached for `measure` (SoftwareX-oriented freeze)
- Active implementation branch: `measure-output-results-layout`

Plan Files
- `MEASURE_PLAN.txt`
- `MEASURE_P2_TRIMMING_SPEC.txt`
- `MEASURE_P3_NUMERICAL_SPEC.txt`
- `MEASURE_P6_VALIDATION_SPEC.txt`
- `MEASURE_P7_TESTS_SPEC.txt`
- `MEASURE_P8_REPRO_SPEC.txt`

Important Branches
- `measure-refactor-base`
- `measure-p2-trimming-spec`
- `measure-p2-trimming-impl`
- `measure-config-loader-fix`
- `measure-p3-numerical-impl`
- `measure-p6-validation-impl`
- `measure-p7-tests-impl`
- `measure-output-results-layout`

Important Commits
- `0426f7d` Add measure refactor plan
- `a699ba1` Define measure trimming specification
- `256cede` Implement configurable measure trimming
- `aaa003b` Align measure sections to mesh spacing
- `4fa1cf1` Use consistent y-level matching tolerance
- `0e8c4e9` Ignore unknown config keys in dataclasses
- `d8ace8a` Define measure P3 numerical specification
- `458120a` Represent analysis windows explicitly
- `4121b2b` Centralize section validity evaluation
- `63da505` Extract section geometry helper
- `f3b4a48` Centralize mesh rounding helpers
- `1307e48` Extract row evaluation helper
- `3234a1a` Validate measure configuration inputs
- `639dcb8` Show cleaner measure config errors
- `8c3262f` Warn when analysis window is adjusted
- `c83667a` Add initial measure tests
- `911f8e2` Add synthetic continuity tests
- `4c61595` Improve measure example reproducibility docs
- `dd9a97d` Organize measure outputs into dedicated folders
- `51d138e` Align surrogate inputs with measure outputs
- `5f0da95` Show cleaner surrogate dependency errors

What P2 Achieved
- Trimming is configurable instead of hard-coded
- `spot size` is treated as laser diameter
- Start and end trimming are independent
- Analysis uses nominal window, observed window, and effective overlap
- Reported `iy` levels are aligned to mesh spacing
- Continuity logic was fixed for real cases where:
  - spot diameter = 75 microns
  - spot diameter = 100 microns
- Extra unknown keys in config no longer break `measure`

What P3 Achieved
- Explicit `AnalysisWindow` representation
- Explicit `SectionSupport` representation
- Shared helper for section validity evaluation
- Shared helper for section geometry extraction
- Shared helper for row evaluation
- Centralized mesh-rounding helpers
- Reduced duplication inside `legacy_funcs.py`

What P6 Achieved So Far
- Early validation of `measure` inputs
- Clearer config-related error messages
- Cleaner CLI output for invalid `measure` configs
- Informative warning when the nominal window is reduced to the observed meltpool window

What P7 Achieved So Far
- Minimal `pytest`-based test infrastructure
- Tests for `MeasureConfig` validation
- Tests for `measure` config parsing helpers
- Tests for mesh snapping and nominal/observed window logic
- Tests for trimming behavior and excessive-trimming errors
- Tests for synthetic continuity, discontinuity, and spurious-level handling
- Current automated result: `20` tests passing locally

What P8 Achieved So Far
- `examples/measure` has been promoted toward the official reproducibility example
- `examples/measure/README.md` was rewritten to act as an operational guide
- `examples/measure/config.yml` was simplified to focus on `measure`
- `measure` outputs are now grouped into:
  - `measure_results/` for primary outputs
  - `measure_aux/` for auxiliary files
  - `measure_work/` for temporary work artifacts
- `surrogate` was aligned with the new `measure` output layout
- missing TensorFlow for `surrogate` now produces a cleaner CLI error

Validated Real Cases
- 75 micron spot diameter: works
- 100 micron spot diameter: works
- These sanity checks were repeated after the recent `P3` refactors and still passed

Current Status
- The `measure` core is more explicit and less fragile than before
- `P2` can be treated as provisionally closed
- `P3` can be treated as provisionally closed
- `P6` has a solid first pass and can be treated as provisionally advanced
- `P7` now has a meaningful automated base around config, numerics, and continuity
- `P8` now includes documentation cleanup and a cleaner on-disk output layout
- `measure` can now be treated as frozen for Level S / SoftwareX-oriented work

Suggested Next Step
- Keep `measure` frozen unless a real bug is found
- Do not start new technical refactors in `measure` before an explicit decision
- When ready, shift focus away from code changes and toward the next planned activity

Notes
- Do not include `.codex` in commits
- Keep using small task branches and frequent commits
